plastix: Differentiable Neural Circuit Simulation
=================================================

Installation
------------

We recommend using `conda` to install binary dependencies for `plastix`,
followed by `pip` for pure python dependencies:

```bash
conda create -n plastix -c conda-forge jax
conda activate plastix
pip install -r requirements.txt
```
